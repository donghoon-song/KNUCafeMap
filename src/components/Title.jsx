import React from 'react';

const Title = ({ title }) => {
    return (
        <div style={{ borderBottom: "0.0625rem solid #e3dddd", fontSize: "2rem", fontFamily: "Noto Sans KR" }}>{title}</div>
    )
}

export default Title;